# ============================================
# Variables d'environnement pour JobAgent
# Copiez ce fichier en .env.local et remplissez les valeurs
# ============================================

# --- Supabase (PostgreSQL) ---
# URL de connexion via PgBouncer (pooling) - utilisee par Prisma en runtime
DATABASE_URL="postgresql://postgres.[REF]:[MOT_DE_PASSE]@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# URL directe sans pooling - utilisee par Prisma pour les migrations
DIRECT_URL="postgresql://postgres.[REF]:@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"
# URL publique du projet Supabase (visible cote client)
NEXT_PUBLIC_SUPABASE_URL="https://[REF].supabase.co"
# Cle anonyme Supabase (visible cote client, acces limite par RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_..."
# Cle service role Supabase (acces admin, NE JAMAIS exposer cote client)
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# --- Better Auth ---
# Secret pour signer les sessions/tokens (generer avec : openssl rand -base64 32)
AUTH_SECRET=""
# URL du serveur d'authentification (en dev : http://localhost:3000)
BETTER_AUTH_URL="http://localhost:3000"
# URL publique d'auth (visible cote client)
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# --- Anthropic (Agent SDK) ---
# Cle API Anthropic pour l'agent IA de scraping
ANTHROPIC_API_KEY="sk-ant-api03-..."

# --- Inngest (Jobs planifies) ---
# Cle d'evenement pour envoyer des events a Inngest
INNGEST_EVENT_KEY=""
# Cle de signature pour valider les webhooks Inngest
INNGEST_SIGNING_KEY=""

# --- Resend (Emails transactionnels) ---
# Cle API Resend pour l'envoi d'emails
RESEND_API_KEY="re_..."
